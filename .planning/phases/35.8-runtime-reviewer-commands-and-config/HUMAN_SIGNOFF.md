# Human Sign-Off

Human reviewer must confirm before public adoption claims:

- `.pramaan.toml` is documented as a private-preview config, not a stable full
  policy language.
- `doctor` is useful for onboarding and does not imply production readiness.
- `verify-pr` and persistent forge updates remain visible future work.

Sign-off status: pending human review.
