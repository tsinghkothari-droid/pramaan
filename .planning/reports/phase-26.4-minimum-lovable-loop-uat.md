# Phase 26.4 Minimum Lovable Loop UAT

Date: 2026-05-21

Command:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run-minimum-lovable-loop.ps1
```

Observed output:

- Ordinary Python unit tests on `examples/vulnerable-python-pr/weakened-test`
  passed.
- Pramaan oracle integrity found one `weakened_assertion` for
  `test_applies_percentage_discount`.
- The oracle command wrote `bundle.manifest.json`.
- `pramaan confidence explain target/pramaan-minimum-lovable` wrote
  `confidence.json`, `confidence.md`, and a confidence receipt with decision
  `fail`.
- `pramaan bundle verify target/pramaan-minimum-lovable` checked 2 receipts and
  3 artifacts successfully.
- `pramaan policy explain target/pramaan-minimum-lovable --profile
  private-preview` failed because `oracle_integrity` and `confidence_vote`
  failed and because the quickstart oracle-only bundle does not include
  `claim_scope` or `sandbox_setup`.

Reviewer 30-second read:

- Open `target/pramaan-minimum-lovable/minimum-lovable-report.md`.
- The first blocker says oracle integrity failed and lists residual oracle
  risks.
- The explanation says CI is green because the test changed from exact discount
  output to a broad positive-value check.

Pass condition:

- A reviewer can explain: "The code is still wrong; CI passed because the test
  oracle was weakened; Pramaan failed the evidence bundle."
- The reviewer does not say Pramaan proved the code correct.
- Missing required stages remain visible as policy failures, not success.

Residual risk:

- This is an oracle-focused demo loop, not a full `pramaan verify` PR run.
- The local report is generated by a PowerShell script; Phase 35.5 still owns
  the Rust-native HTML/Markdown report commands.
