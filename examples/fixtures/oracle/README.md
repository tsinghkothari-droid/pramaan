# Oracle fixtures

The `base` and `head` directories model oracle weakening while ordinary tests could still pass.
They cover deleted tests, skip or todo additions, parametrized case reduction, Python and JS/TS assertion weakening, and fixture or snapshot changes.
