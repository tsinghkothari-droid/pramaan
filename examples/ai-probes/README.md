# AI Probe Examples

This directory holds small, inspectable examples for Phase 28.25.

The current example shape is a provider-neutral probe plan: Pramaan proposes
what evidence to seek next, records the prompt hash and risk IDs, and keeps the
probe pending until sandbox execution exists.
